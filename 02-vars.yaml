- name: Demo on variables
  hosts: all
  vars:
    URL: https:google.com
  tasks:
    - name: Printing the global variable
      ansible.builtin.debug:
        msg : "{{URL}}"
    
    - name: Printing the global variable
      ansible.builtin.debug:
        msg : "{{URL}}"
      vars:
        URL: https:local.com